<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/stylesheets/login.css">
</head>
<body>
  <div class="container">
    <div class="content">
      <!--pinterest logo-->
      <img src="https://i.pinimg.com/originals/d3/d1/75/d3d175e560ae133f1ed5cd4ec173751a.png" alt="pin logo" class="img1" />
      <p class="header">Log in to see more</p>
      
      <!-- <% if ( failureMsg ) { %>
        <h1 class="failureMsg"><%= failureMsg %></h1>
        <%}%> />  -->

  <form action="/login" method="post">

    <input 
    name="username" 
    type="text" 
    placeholder="Username"
    class="<%= failureMsg.length > 0 ? 'err-msg' : 'detail' %>" 
    value= "<%= failureMsg.length > 0 ? failureMsg : '' %>"
     />
     <br/>

    <input 
    name="password" 
    type="<%= failureMsg.length > 0 ? 'text' : 'password' %>" 
    placeholder="Password" 
    class="<%= failureMsg.length > 0 ? 'err-msg' : 'detail' %>" 
    value= "<%= failureMsg.length > 0 ? failureMsg : '' %>" 
    />
   
    <button class="btn int">Log In Account</button>   
  </form>

      <a href="#" class="forgot">Forgot your password?</a>  
      <p class="or">OR</p>
      <button class="btn fbk">
        <i
          class="fab fa-facebook fa-lg"
          style="color: white; padding-right: 10px"
        ></i
        ><a href="#">Continue with Facebook</a></button
      ><br />
      <button class="btn ggl">
        <i
          class="fab fa-google"
          style="color: rgb(11, 241, 22); padding-right: 10px"
        ></i
        ><a href="#">Continue with Google</a>
      </button>

      <footer>
        <p>
          By continuing, you agree to Pinterest's
          <b>Terms of Service, Privacy policy.</b>
        </p>
        <hr />
        <p>Not on Pinterest yet? Sign up</p>
      </footer>
    </div>
  </div>
  <a class="upbtn" href="/">Sign up</a>

</body>
</html>











